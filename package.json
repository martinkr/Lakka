{
  "name": "lakka",
  "version": "2.0.0-alpha.1",
  "description": "lakka. An advanced client-side cache.",
  "main": "./app/main.js",
  "license": "MIT",
  "engines": {
    "node": ">=9.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/martinkr/lakka.git"
  },
  "homepage": "https://github.com/martinkr/lakka",
  "issues": "https://github.com/martinkr/lakka/issues",
  "author": "Martin Krause <github@mkrause.info> (http://martinkr.github.io)",
  "keywords": [],
  "scripts": {
    "eslint": "eslint ./app/**/*.js",
    "mocha": "./node_modules/mocha/bin/mocha --reporter spec",
    "nyc": "nyc --clean ./node_modules/.bin/mocha",
    "coverage": "nyc check-coverage --lines 100 --functions 100 --branches 100 --statements 100",
    "report": "nyc report --reporter=lcov --reporter=html",
    "test": "yarn eslint && yarn nyc && yarn report && yarn coverage",
    "mocha-watch": "mocha --reporter min -w",
    "eslint-watch": "esw --watch"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-loader": "^7.1.2",
    "babel-minify-webpack-plugin": "^0.2.0",
    "babel-preset-env": "^1.6.1",
    "chai": "^4.1.1",
    "eslint": "^4.4.1",
    "eslint-watch": "3.1.2",
    "expose-loader": "^0.7.4",
    "fs-extra-plus": "0.1.3",
    "mocha": "^3.5.0",
    "nyc": "^11.2.1",
    "proxyquire": "^1.8.0",
    "sinon": "^4.0.2",
    "sinon-chai": "^2.14.0",
    "uglifyjs-webpack-plugin": "^1.1.6",
    "webpack": "^3.10.0"
  },
  "bugs": {
    "url": "https://github.com/martinkr/lakka/issues"
  },
  "directories": {
    "test": "test"
  }
}
