{
  "name": "lakka-core",
  "version": "1.0.3",
  "description": "lakka. An advanced client-side cache.",
  "main": "./app/main.js",
  "license": "MIT",
  "engines": {
    "node": ">=8.5.5"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/martinkr/lakka-core"
  },
  "homepage": "https://github.com/martinkr/lakka-core",
  "issues": "https://github.com/martinkr/lakka-core/issues",
  "author": "Martin Krause <github@mkrause.info> (http://martinkr.github.io)",
  "keywords": [],
  "scripts": {
    "eslint": "./node_modules/.bin/eslint ./app/**/*.js",
    "mocha": "./node_modules/mocha/bin/mocha --reporter spec",
    "nyc": "./node_modules/.bin/nyc --clean ./node_modules/.bin/mocha",
    "coverage": "./node_modules/.bin/nyc check-coverage --lines 100 --functions 100 --branches 100 --statements 100",
    "report": "./node_modules/.bin/nyc report --reporter=lcov --reporter=html",
    "test": "yarn eslint && yarn nyc && yarn report && yarn coverage",
    "mocha-watch": "./node_modules/.bin/mocha --reporter min -w",
    "eslint-watch": "./node_modules/.bin/esw --watch"
  },
  "dependencies": {
    "foreverjs": "^1.1.241"
  },
  "devDependencies": {
    "forever": "*",
    "chai": "^4.1.1",
    "eslint": "^4.4.1",
    "eslint-watch": "3.1.2",
    "fs-extra-plus": "0.1.3",
    "mocha": "^3.5.0",
    "nyc": "^11.2.1",
    "proxyquire": "^1.8.0",
    "sinon": "^4.0.2",
    "sinon-chai": "^2.14.0"
  }
}
